import { abstract } from './';

jest.setTimeout(50000);

describe('please', () => {
  it('write some tests', async () => {
    // abstract.orderbook.watch(['MLN/ETH']).subscribe(message => {
    //   console.log(message);
    // });
    // return new Promise(() => {});
  });
});
